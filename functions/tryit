const caculateArea = (num1, num2) => {
    console.log('area of a triangle with the width ' + (num1 * num2)/2)
}

caculateArea(38, 18);

const planetHasWater = (planet) =>{
    if(planet === "Earth" || planet === "Mars"){
        return true;
    }else{
        return false;
    }
}

const caculateAreaCircle = (radius) => {
    return 'The area of a circle with the radius of '
}